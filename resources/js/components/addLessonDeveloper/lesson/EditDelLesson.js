import { forwardRef } from "react"

const EditDelLesson=(props , ref)=>{
    return (
        <section className="SAED_content">
            edit delete lesson
        </section>
    )
}

export default forwardRef(EditDelLesson);
// # جهت رفتن به یک جای خاص در صفحه از این هوک استفاده می شود 
const useScrollTo = (offSet)=>{
        console.log(offSet);
        // const offsetElement = $("#"+id).offset();
        // $('html,body').animate({ scrollTop: offsetElement.top - 40 }, 'slow');
}
export default useScrollTo;